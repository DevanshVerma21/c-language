//WAP in C to print prime numbers only from an array
#include <stdio.h>

int main(){
    int n;
    printf("Enter the values of n:");
    scanf("%d",&n);

    int arr[n];
    printf("values:\n");
    for(int i =0;i<n;i++){
        scanf("%d",arr[i]);
    }

    
}